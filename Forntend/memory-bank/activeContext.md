# Active Context — EduFast

## Current State
- **Project Initialized**: A Vite 8 + React 19 codebase with core shell components (`App.jsx`, `main.jsx`, `components/`, `pages/`).
- **Memory Bank Setup**: Initiated the Memory Bank configuration (`AGENTS.md` and the `memory-bank` directory).

## Immediate Objectives
- Align coding assistant behavior with `AGENTS.md` instructions.
- Ensure all current and new code runs cleanly under React 19.
- Maintain clean build outputs and run linters with `npm run lint`.

## Active Work & Tasks
1. [ ] Check existing `App.jsx` and components for any immediate fixes or cleanups.
2. [ ] Test the dev server with `npm run dev`.
3. [ ] Resolve any initial lint warnings or build blocks.
